// dev-kiro layout: yazi (left) + kiro (right-top) + terminal (right-bottom)
layout {
    pane split_direction="vertical" {
        pane size="15%" command="yazi"
        pane split_direction="horizontal" {
            pane size="60%" command="kiro-cli" focus=true {
                args "chat"
            }
            pane size="40%"
        }
    }
}
