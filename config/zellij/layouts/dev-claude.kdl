// dev-claude layout: yazi (left) + claude (right-top) + terminal (right-bottom)
layout {
    pane split_direction="vertical" {
        pane size="15%" command="yazi"
        pane split_direction="horizontal" {
            pane size="60%" command="claude" focus=true
            pane size="40%"
        }
    }
}
