[mgr]
ratio = [0, 1, 0]
sort_by = "natural"
show_hidden = true

[opener]
edit = [
    { run = 'zellij run -f -c --width 80% --height 80% -- nvim %s', orphan = true, desc = "Edit in floating pane", for = "unix" },
]
